$(document).ready(function(){
		    $("#visit").click(function(e) {
				      e.preventDefault();
				      // Get the page combined with the visit_type
				      $.get(this.href + "/" + $("#visit_type option:selected")[0].attr("value"));
				    }
				    );
		  }
		  );

